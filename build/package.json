{
  "name": "build",
  "version": "0.1.0",
  "description": "Build for Charlie Demo",
  "scripts": {
    "build-server": "cd ../server && npm run tsc-prod && cd ../build && webpack",
    "build-client": "cd ../client && npm run build"
  },
  "devDependencies": {
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  }
}